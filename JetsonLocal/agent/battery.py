from typing import Dict, Any


def read_battery_status() -> Dict[str, Any]:
    # Placeholder for now until your real battery telemetry is wired in
    return {
        "battery_percent": None,
        "battery_voltage": None,
        "charging": None,
    }